# Dashboard sous Streamlit

## Lancement de l'application

Contrairement à un script Python classique, une application Streamlit ne se lance pas avec la commande `python app.py`. Elle utilise son propre serveur de développement.

Pour lancer le dashboard, utilise la commande suivante dans ton terminal :

```bash
streamlit run nom_de_ton_fichier.py